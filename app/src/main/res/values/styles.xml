<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="portalTitle">
        <item name="android:textSize">30dp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="portalSubtitle">
        <item name="android:textSize">25dp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="portalLoginMessageBox">
        <item name="android:padding">15dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>

    <style name="portalLoginMessageBoxTitle">
        <item name="android:textSize">20dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="portalLoginMessageBoxText">
        <item name="android:textSize">12dp</item>
    </style>

    <style name="portalInput">
        <item name="android:background">@drawable/portal_input_shape</item>
    </style>

</resources>
